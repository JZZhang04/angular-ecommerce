spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ecommerceapp}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ecommerceapp}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.data.rest.base-path=/api

anthropic.api.key=${ANTHROPIC_API_KEY:}

## issuer url must ends with "/"
okta.oauth2.issuer=${OKTA_OAUTH2_ISSUER:https://dev-duuf521z85fwio7f.us.auth0.com/}
okta.oauth2.client-id=${OKTA_OAUTH2_CLIENT_ID:qyoaZgzDttU4n1OvfPzocATlk6WKaBcw}
okta.oauth2.audience=${OKTA_OAUTH2_AUDIENCE:http://localhost:8080}

# Global CORS configuration
allowed.origins=${ALLOWED_ORIGINS:http://localhost:4200}

server.port=${PORT:8080}
